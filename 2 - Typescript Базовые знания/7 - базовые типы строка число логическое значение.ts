// 7 - базовые типы строка число логическое значение

const isBirthday: boolean = true;
let age: number;
const userName: string = "John";

age = 40;

if (isBirthday) console.log(`Congrats ${userName.toLocaleUpperCase()}, age: ${age + 1}`);